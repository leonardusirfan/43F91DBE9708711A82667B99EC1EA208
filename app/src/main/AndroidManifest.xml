<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.net.gmedia.perkasaapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeActionBar">
        <activity
            android:name=".ActivitySplashScreen"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityLogin"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ActivityHome"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".ActivityOrderPerdana1" />
        <activity android:name=".ActivityOrderMkios1" />
        <activity android:name=".ActivityPenjualanHariIni" />
        <activity android:name=".ActivityOrderMkios2" />
        <activity android:name=".ActivityOrderPerdana2" />
        <activity android:name=".ActivityStokPerdana" />
        <activity android:name=".ActivityPiutang" />
        <activity
            android:name=".ActivityKomplain"
            android:label="@string/title_activity_komplain"
            android:theme="@style/AppThemeActionBar.NoActionBar" />
        <activity android:name=".ActivityKomplain2" />
        <activity android:name=".ActivityOrderTcash1" />
        <activity android:name=".ActivityOrderTcash2" />
        <activity android:name=".ActivityOrderTcash3" />
        <activity android:name=".ActivityVerifikasiOutlet1" />
        <activity android:name=".ActivityVerifikasiOutlet2" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>

</manifest>